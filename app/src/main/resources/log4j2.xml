<?xml version="1.0" encoding="utf-8"?>
<Configuration status="INFO" packages="com.github.starter.core.logging">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Property>
        <Property name="JSON_LOG_PATTERN">{"timestamp":"%d{ISO8601}", "level":"%level", "thread":"%thread", "class":"%logger{36}", "message":"%message", "throwable":"%throwable{full}"}%n</Property>
        <Property name="APP_NAME">spring-starter-java</Property>
        <Property name="APP_LOG_ROOT">logs</Property>
    </Properties>
    
    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}" />
        </Console>
        
        <!-- JSON Console Appender for structured logging -->
        <Console name="JsonConsole" target="SYSTEM_OUT">
            <PatternLayout pattern="${JSON_LOG_PATTERN}" />
        </Console>
        
        <!-- Rolling File Appender -->
        <RollingFile name="RollingFile" 
                     fileName="${APP_LOG_ROOT}/${APP_NAME}.log" 
                     filePattern="${APP_LOG_ROOT}/${APP_NAME}-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${LOG_PATTERN}" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB" />
                <TimeBasedTriggeringPolicy interval="1" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
        
        <!-- JSON File Appender for structured logging -->
        <RollingFile name="JsonRollingFile" 
                     fileName="${APP_LOG_ROOT}/${APP_NAME}-json.log" 
                     filePattern="${APP_LOG_ROOT}/${APP_NAME}-json-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${JSON_LOG_PATTERN}" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB" />
                <TimeBasedTriggeringPolicy interval="1" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
    </Appenders>
    
    <Loggers>
        <!-- Application Loggers -->
        <Logger name="com.github.starter" level="DEBUG" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="JsonConsole" />
            <AppenderRef ref="RollingFile" />
            <AppenderRef ref="JsonRollingFile" />
        </Logger>
        
        <!-- Spring Framework Loggers -->
        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Logger>
        
        <!-- OpenTelemetry Loggers -->
        <Logger name="io.opentelemetry" level="INFO" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Logger>
        
        <!-- Root Logger -->
        <Root level="INFO" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>
</Configuration>

