# Load Testing Configuration
# The module/domain being tested (affects package names)
testModule=pokemon

# Default simulation class (can be overridden via -Psimulation=<class>)
simulation=com.github.starter.pokemon.SimulationEntrypoint
